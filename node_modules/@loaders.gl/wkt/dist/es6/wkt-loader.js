import parseWKT from './lib/parse-wkt';
const VERSION = typeof "2.3.9" !== 'undefined' ? "2.3.9" : 'latest';
export const WKTWorkerLoader = {
  id: 'wkt',
  name: 'WKT',
  version: VERSION,
  extensions: ['wkt'],
  mimeTypes: ['text/plain'],
  category: 'geometry',
  text: true,
  options: {
    wkt: {
      workerUrl: "https://unpkg.com/@loaders.gl/wkt@".concat(VERSION, "/dist/wkt-loader.worker.js")
    }
  }
};
export const WKTLoader = { ...WKTWorkerLoader,
  parse: async (arrayBuffer, options) => parseWKT(new TextDecoder().decode(arrayBuffer)),
  parseTextSync: parseWKT
};
//# sourceMappingURL=wkt-loader.js.map